endpoints {
  http {
    host: "0.0.0.0"
    host: ${?HTTP_ENDPOINT_HOST}
    port: 8080
    port: ${?HTTP_ENDPOINT_PORT}
  }
}

alertmanager {
  balemessenger {
    api-token: "alerthook"
    api-token: ${?ALERTMANAGER_BALEMESSENGER_API_TOKEN}
  }
}
